// ═══════════════════════════════════════════════════════════════════════════
// LAMBDA_TEST.IFA - Arrow Functions / Lambdas
// Demonstrates BOTH Yoruba and English syntax
// ═══════════════════════════════════════════════════════════════════════════

ìbà Irosu;   // Yoruba import
import Ogunda; // English import

// ═══════════════════════════════════════════════════════════════════════════
// 1. DEFINE LAMBDAS
// ═══════════════════════════════════════════════════════════════════════════

// Yoruba variable with lambda
// padà = return
ayanmọ double = (x) -> { padà x * 2; };

// English variable with lambda
// return = return (English alias)
let triple = (n) -> { return n * 3; };

// ═══════════════════════════════════════════════════════════════════════════
// 2. CALL LAMBDAS
// ═══════════════════════════════════════════════════════════════════════════

ayanmọ result = double(5);
Irosu.fo("Double of 5: " + result);  // 10

let triple_result = triple(7);
Irosu.fo("Triple of 7: " + triple_result);  // 21

// ═══════════════════════════════════════════════════════════════════════════
// 3. HIGHER-ORDER FUNCTIONS
// ═══════════════════════════════════════════════════════════════════════════

ayanmọ numbers = [1, 2, 3, 4, 5];

// Map: Apply lambda to each element
// Yoruba: map
let doubled = Ogunda.map(numbers, double);
Irosu.fo("Doubled list: " + doubled);  // [2, 4, 6, 8, 10]

// Filter: Using inline lambda with Yoruba return
ayanmọ even = Ogunda.filter(numbers, (x) -> { padà x % 2 == 0; });
Irosu.fo("Even numbers: " + even);  // [2, 4]

// ═══════════════════════════════════════════════════════════════════════════
// 4. INLINE LAMBDAS
// ═══════════════════════════════════════════════════════════════════════════

// Directly in function call
Irosu.fo("Square of 4: " + ((x) -> { padà x * x; })(4));  // 16

àṣẹ;