# Work Log — 2026-03-21

This entry captures the work done to make `IFA_LANG_RUNTIME_SPEC.md` internally consistent and to unblock VM implementers by completing the opcode table.

## Spec work (`IFA_LANG_RUNTIME_SPEC.md`)

- Completed §17.2 as a full opcode table (byte, operands, stack effect, semantics) and added it to the ToC.
- Removed the accidental duplication spill that made §17.1 inconsistent.
- Ratified keywords globally:
  - `ayanfe` (const) — removed `aiyeyi`
  - `nipari` (finally) — removed `nígbàtí`
  - `reti` (await) — replaced legacy `duro fun`
  - `iba` is the canonical import keyword.
- Restored Appendix A and Appendix B; added §45/§46 to the ToC; downgraded §46 to `[PARTIAL]`.
- Fixed targeted inconsistencies/typos:
  - §4.8 code fence
  - §23.2 borrow example uses `ailewu`
  - §25.3.5 duplicate header → §25.3.6
  - §27.6 `@aṣẹ` → `#[aṣẹ]` (deprecated form noted)
  - §33 conformance note: `Iwe.toml` is not a legacy fallback for `ifa.toml`
  - §38.5 doc-test parsing note (comment-text parsing)
  - §19.2 overflow: removed/clarified `OverflowError`.
- Specified `Sys` dispatch in §13.17:
  - encoded as `CallOdu` with `domain_id = 29`
  - explicit call path via `OduRegistry` / `StdRegistry`.
- Added missing opcodes required by other sections:
  - closures: `MakeClosure`, `LoadUpvalue`, `StoreUpvalue`
  - tail calls: `TailCall`
  - objects: `GetField`, `SetField`
  - fixed `CallOdu` operand encoding mismatch (`u8 + u16 + u8`, not a single `u32`).
- Added `AJOSE_NON_OBSERVABLE` and `ASYNC_NOT_AVAILABLE` to §22.3 and Appendix C.

## Code work (`crates/ifa-bytecode`)

- Updated `crates/ifa-bytecode/src/lib.rs` to add the missing opcodes and align operand sizing with the spec.
- Added `tools/check_spec_opcode_sync.py` to keep §17.2 and `OpCode` byte assignments in sync.

## VM viability work (`crates/ifa-core`)

- Implemented `TailCall` emission for `pada f(...)` where the return value is a direct function call (`crates/ifa-core/src/compiler.rs`).
- Implemented `OpCode::TailCall` execution by reusing the current call frame (`crates/ifa-core/src/vm.rs`).
- Added a compiler-level regression test asserting that `TailCall` is emitted (`crates/ifa-core/tests/tailcall_tests.rs`).

## MUST semantics + executable checks

- Enforced `ayanfe` immutability in both:
  - AST interpreter (`crates/ifa-core/src/interpreter/core.rs`, `crates/ifa-core/src/interpreter/environment.rs`)
  - Bytecode compiler (rejects reassignment at compile time) (`crates/ifa-core/src/compiler.rs`)
- Added conformance harnesses driven by `# expect:` directives:
  - AST: `crates/ifa-core/tests/conformance_ast_tests.rs` runs `tests/conformance/ast/*.ifa`
  - VM: `crates/ifa-core/tests/conformance_vm_tests.rs` runs `tests/conformance/vm/*.ifa`
- Seeded MUST tests for truthiness, operand-return short-circuit, and const immutability under:
  - `tests/conformance/ast/`
  - `tests/conformance/vm/`

## Transpiler hard-fail instead of silent wrong output

- Replaced “emit a comment and continue” behavior for try/catch with a Rust `compile_error!(...)` (`crates/ifa-core/src/transpiler/statements.rs`).
- Removed a transpiler `panic!()` path for dereference assignment by emitting `compile_error!(...)` instead (`crates/ifa-core/src/transpiler/statements.rs`).

## Result

- The spec’s §17.2 opcode table and the `ifa-bytecode` `OpCode` enum now match exactly (count and byte values).
