# Ifá-Lang Dependencies
# Install with: pip install -r requirements.txt

# Parser (formal EBNF grammar)
lark>=1.1.0

# TOML parsing (for ifa.toml and Ọjà package manager)
toml>=0.10.0

# Testing
pytest>=7.0.0

# Optional: Type checking
# mypy>=1.0.0
