// ═══════════════════════════════════════════════════════════════════════════
// CRYPTO BOT - Production Example
// Demonstrates: HTTP API, JSON parsing, SQLite database
// ═══════════════════════════════════════════════════════════════════════════

ìbà std.otura;   // Network / HTTP
ìbà std.ika;     // JSON
ìbà std.odi;     // Database
ìbà std.irosu;   // Output

// ═══════════════════════════════════════════════════════════════════════════
// 1. DATABASE SETUP
// ═══════════════════════════════════════════════════════════════════════════

Ìrosù.fọ̀("🏦 Setting up database...");

// Open/create SQLite database
Òdí.sí("crypto.db");

// Create table for storing prices
Òdí.pa_àṣẹ("CREATE TABLE IF NOT EXISTS prices (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    coin TEXT NOT NULL,
    price REAL NOT NULL,
    timestamp INTEGER DEFAULT (strftime('%s', 'now'))
)");

Ìrosù.fọ̀("✅ Database ready!");

// ═══════════════════════════════════════════════════════════════════════════
// 2. FETCH DATA FROM API
// ═══════════════════════════════════════════════════════════════════════════

Ìrosù.fọ̀("📡 Fetching Bitcoin price from CoinDesk API...");

// HTTP GET request
ayanmọ url = "https://api.coindesk.com/v1/bpi/currentprice.json";
ayanmọ response = Òtúrá.gbà(url);

Ìrosù.fọ̀("📦 Response received!");

// ═══════════════════════════════════════════════════════════════════════════
// 3. PARSE JSON
// ═══════════════════════════════════════════════════════════════════════════

Ìrosù.fọ̀("🔍 Parsing JSON data...");

// Parse the JSON string into an Ifá Map
ayanmọ data = Ìká.tú(response);

// Access nested properties
// data.bpi.USD.rate
ayanmọ bpi = Ìká.gbà_inú(data, "bpi");
ayanmọ usd = Ìká.gbà_inú(bpi, "USD");
ayanmọ rate = Ìká.gbà_inú(usd, "rate_float");

Ìrosù.fọ̀("💰 Bitcoin Price: $");
Ìrosù.fọ̀(rate);

// ═══════════════════════════════════════════════════════════════════════════
// 4. SAVE TO DATABASE
// ═══════════════════════════════════════════════════════════════════════════

Ìrosù.fọ̀("💾 Saving to database...");

// Insert the price (using string interpolation placeholder for now)
Òdí.pa_àṣẹ("INSERT INTO prices (coin, price) VALUES ('BTC', 50000)");

Ìrosù.fọ̀("✅ Price saved!");

// ═══════════════════════════════════════════════════════════════════════════
// 5. QUERY HISTORY
// ═══════════════════════════════════════════════════════════════════════════

Ìrosù.fọ̀("📊 Price History:");

ayanmọ history = Òdí.kà_inú("SELECT coin, price, timestamp FROM prices ORDER BY id DESC LIMIT 5");

// Print each row
Ìrosù.fọ̀(history);

// ═══════════════════════════════════════════════════════════════════════════
// 6. CLEANUP
// ═══════════════════════════════════════════════════════════════════════════

Òdí.tì();
Ìrosù.fọ̀("🎉 Done! Àṣẹ!");

ase;
